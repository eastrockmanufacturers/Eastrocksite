services:
  - type: web
    name: eastrock-backend
    env: node
    plan: free
    # Use owner/repo format (required by Render Blueprints)
    repo: eastrockmanufacturers/Eastrocksite
    branch: gh-pages
    buildCommand: npm install
    startCommand: npm start
